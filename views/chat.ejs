<div class="chat-container">
  <main class="chat-main">
    <div class="chat-sidebar">
      <div class="profile-sidebar">
        <img class="profile-img" src="<%= img %>" alt="<%= displayName %>" width="100" height="100">
        <h4 id="userinfo"></h4>
      </div>
      <!-- <h3><i class="fas fa-comments"></i> Room Name:</h3> -->
      <!-- <h2 id="room-name"></h2> -->
      <!-- <h3><i class="fas fa-users"></i> Users</h3> -->
      <div class="users-list">
        <ul id="users"></ul>
      </div>
      
    </div>
    <div class="right-panel">
        <header class="chat-header">
          <h1><i class="fas fa-smile"></i> oChat</h1>
          <a href="/logout" class="btn">Leave Chat</a>
        </header>
        <div id="chat-window">
          <div class="chat-messages"></div>
          <div class="chat-form-container">
            <form id="chat-form" method="post">
              <input id="msg" type="text" placeholder="Enter Message" required autocomplete="off" />
              <button class="btn"><i class="fas fa-paper-plane"></i> Send</button>
            </form>
          </div>
        </div>
    </div>
       
  </main>

</div>
<script>
  let username = "<%= displayName %>";
  let room = "<%= room %>";
  let img = "<%= img %>";
  let roomid = "<%= roomid %>";
  let userId = "<%= userId %>";
  let email  = "<%= email %>";
</script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/qs/6.9.4/qs.min.js"
  integrity="sha256-ijqLssetA1NMNjZ+w1zaUafQQLm4IW5OoM8O+4tatME=" crossorigin="anonymous"></script>
<script src="/socket.io/socket.io.js"></script>
<script src="js/main.js"></script>
